spring:
  cloud:
    vault:
      uri: http://localhost:8200
      token: dev-only-token
  config:
    import: vault:///vault-sample

  datasource:
    url:  jdbc:mysql://localhost:3306/airbnb
    username: root
    password: liviu1986
  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl